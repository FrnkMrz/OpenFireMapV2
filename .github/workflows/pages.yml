- name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # WICHTIG: Hier definieren wir, dass nur der Inhalt von /docs veröffentlicht wird.
          # Das macht /docs zum "Root" der Webseite.
          path: './docs'
